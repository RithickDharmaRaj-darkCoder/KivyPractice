<MyLayouts>:
    BoxLayout:
        orientation: 'vertical'
        size: root.width, root.height
        spacing: 20
        padding: 20

        Button:
            text: 'Check'

        Button:
            text: 'Submit'
            size_hint: (.1, .1)
            #width: 100
            #height: 100
            pos_hint: {'center_x': .5}
        Button:
            text: 'OPEN'
